version: '3.8'
services:
  ontmqlapp:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: omrchecker
    volumes:
      - ./:/app
    working_dir: /app
    stdin_open: true
    tty: true
    restart: "no"
    entrypoint: /bin/bash
    ports:
      - "3000:3000"

